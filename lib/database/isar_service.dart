import 'package:isar/isar.dart';

class IsarService {
  static IsarService? instance;
  final Isar isar;

  IsarService(this.isar);

  // Add methods for local database operations
  // This will be expanded as we add models
}

